# tech-interview-notes — プロジェクト方針

このリポジトリは **技術面接の振り返り教材** をためる場所。
面接で答えられなかった / 詰まったトピックを、後で見返せる教材として整理する。

---

## 言語

- 説明・解説・コメントはすべて**日本語**で書く
- コード / コマンド例の中身は通常通り英語のままで OK（変数名や関数名は変えない）

## ディレクトリ構成

```
tech-interview-notes/
├── CLAUDE.md          # このファイル
├── README.md          # 索引（追加トピックごとに更新）
└── docs/
    └── <topic>.md     # 1 トピック 1 ファイル
```

新トピックは `docs/<topic-kebab-case>.md` に作る。

## 教材ファイルのテンプレート

各 `docs/*.md` は以下のセクションを**この順で**含める：

1. **1分サマリ** — 面接で 15〜30 秒で答えるならコレ
2. **本質の理解** — なぜ存在するか / 何を解決するか
3. **対比表** — 違いを一目で比較できる表
4. **ハンズオン** — コピペで動く実コード/実コマンド（実行手順込み、Mac 環境前提）
5. **よくある誤解と落とし穴** — 面接で突っ込まれるポイント
6. **章末 Q&A** — `<details>` で答えを隠して、自分で答えてから確認できる形式
7. **模擬面接質問** — 3〜5 問の口頭想定問題

## 運用ルール

### 新トピック追加時
1. `docs/<topic>.md` をテンプレに従って作成
2. `README.md` の索引テーブルに 1 行追加

### 模擬面接モード
ユーザーが「`/mock <topic>` 出して」「`<topic>` の模擬面接やって」と頼んだら:

1. 対応する `docs/<topic>.md` の **模擬面接質問** から 1 問を出題
2. ユーザーが**口頭想定で**回答するのを待つ
3. 回答に対して以下を返す:
   - **スコア**（5 段階: 1=触れられず / 2=断片的 / 3=要点は押さえている / 4=深い理解 / 5=面接官に追加で唸らせるレベル）
   - **良かった点**
   - **抜けていたキーワード**
   - **モデル回答**（教材本文を踏まえた 2〜4 行の模範解答）
4. 1 問終わったら次の問題に進むかユーザーに確認

### 「次は何をやる？」と聞かれたら
- まだファイル化されていないトピックを `README.md` から推測して提案
- 過去に低スコアだった分野があれば優先的に復習を提案
- ユーザーが直近で触れた言語/技術スタック寄りのトピックを優先

## このリポジトリの主の状況

- **2026 年の技術面接対策中**
- 初回の振り返り対象: TCP/UDP、INDEX、タプルとリスト
- **学習方針**: 暗記ではなく「**なぜそうなっているか**」の理解を重視する。教材を書く時もこの方針を守ること
- 説明では「結論 → なぜ → 例」の順で書く（面接で話す時の思考フローと同じ）

## Claude Code 出力スタイルとの連携

このリポジトリで作業する時、ユーザーは以下の出力スタイルを使い分けることがある:

- `/output-style explanatory` — 解説重視。新トピック追加時の説明に向く
- `/output-style learning` — `TODO(human)` マーカーを使った穴埋め学習モード。ユーザー自身に書かせたい時に使う

ユーザーから「学習モードで」と頼まれたら、教材中に意図的に空欄や `???` を残し、ユーザーが埋める形に書き換えること。
